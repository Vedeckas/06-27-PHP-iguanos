function Animal ({type}){

    if (type === 'racoon'){
        return (
            <div>
                <h1>RACOON</h1>
        </div>
        );
    } else if (type === 'cat'){
     <h1>Animal</h1>
        }
};

export default Animal